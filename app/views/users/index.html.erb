<p style="color: green"><%= notice %></p>

<h1>Users</h1>

<table>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Redact</th>
    </tr>
  </thead>

    <% @users.each do |user| %>
      <tr>
        <td><%= user.username %></td>
        <td><%= user.email %></td>
        <td><%= link_to 'Show', user %></td>
      </tr>
    <% end %>
</table>

<%= link_to "New user", new_user_path %>
<%= link_to "To calculation", calc_input_path %>
<%= link_to "Show XML", users_xml_path %>
<%= button_to "Destroy this user", User.find_by_id(session[:current_user_id]), method: :delete %>
